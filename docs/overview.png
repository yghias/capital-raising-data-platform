This is a text placeholder for `overview.png` because a real PNG was not rendered in this environment.

Use the Mermaid source below to generate an image in a future pass if needed.

```mermaid
flowchart LR
    A["Sources"] --> B["Raw Landing"]
    B --> C["Standardized Layer"]
    C --> D["Canonical Layer"]
    D --> E["Analytical Marts"]
    E --> F["Dashboards and Scoring"]
```
