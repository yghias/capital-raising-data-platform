Diagram source for `overview.png`. This repository stores Mermaid text here so the architecture remains reviewable without a binary image renderer.

```mermaid
flowchart LR
    A["Sources"] --> B["Raw Landing"]
    B --> C["Standardized Layer"]
    C --> D["Canonical Layer"]
    D --> E["Analytical Marts"]
    E --> F["Dashboards and Scoring"]
```
